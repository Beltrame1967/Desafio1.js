const nota1 = 7.5
const nota2 = 8.0
const nota3 = 9.0

const calcMedia = (nota1 + nota2 + nota3)/3

if(calcMedia >= 7) {
    console.log(`O aluno: ${nomeAluno}. Foi aprovado com media final de ${mediaFinal.tofixed(2)}`)};

    else if(calcMedia >= 5 && calcMedia <=6)
    { 
    console.log(`O aluno: ${nomeAluno}. Está de recuperação com médis final de: ${mediaFinal.toFixed(2)}`)};
    else {
        console.log(`Àluno reprovado`);                                                                                        
    } 






























        console.log("Reprovado")}
            
    else{
                console.log("Exame")
            }

